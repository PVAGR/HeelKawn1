---
description: Read HEELKAWN.txt at start; append to log when project is updated
alwaysApply: true
---

# HeelKawn memory (exact user request)

- At the start of work in this repository: read `HEELKAWN.txt` in the project root so you know where we left off.
- After every time you update the project (code, scenes, or behavior): edit `HEELKAWN.txt`—refresh **WHERE WE LEFT OFF** and add a line to the **LOG** (newest first). Do not rely on chat; it does not persist across sessions.
- The file is plain text, not only chat: `HEELKAWN.txt`.
